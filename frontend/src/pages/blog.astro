---
import Layout from '../layouts/Layout.astro';

const posts = [
  {
    title: 'Introducing nex v1.0',
    date: '2023-10-25',
    excerpt: 'We are excited to announce the first stable release of nex, the package manager for developer tools.',
    slug: '#'
  },
  {
    title: 'How to create your first nex package',
    date: '2023-11-05',
    excerpt: 'Learn how to package your favorite CLI tool and share it with the world using nex.',
    slug: '#'
  },
  {
    title: 'Nex vs. The World: A Comparison',
    date: '2023-11-15',
    excerpt: 'A deep dive into how nex compares to other package managers like Homebrew, Scoop, and Chocolatey.',
    slug: '#'
  }
];
---

<Layout title="Blog" showSearch={false}>
  <div class="container page-content">
    <header class="page-header">
      <h1>Blog & Changelog</h1>
      <p class="lead">Latest news, updates, and tutorials from the nex team.</p>
    </header>

    <div class="posts-list">
      {posts.map(post => (
        <article class="post-card">
          <div class="post-meta">{post.date}</div>
          <h2 class="post-title">
            <a href={post.slug}>{post.title}</a>
          </h2>
          <p class="post-excerpt">{post.excerpt}</p>
          <a href={post.slug} class="read-more">Read more &rarr;</a>
        </article>
      ))}
    </div>
  </div>
</Layout>

<style>
  .page-content {
    padding-top: 3rem;
    padding-bottom: 4rem;
    max-width: 800px;
  }
  
  .page-header {
    margin-bottom: 3rem;
    text-align: center;
  }
  
  .page-header h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: var(--nex-black);
  }
  
  .lead {
    font-size: 1.2rem;
    color: var(--nex-gray);
  }
  
  .posts-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  
  .post-card {
    background: var(--nex-white);
    padding: 2rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow-sm);
    border: 1px solid var(--nex-border);
    transition: box-shadow 0.2s;
  }
  
  .post-card:hover {
    box-shadow: var(--shadow-md);
  }
  
  .post-meta {
    font-size: 0.9rem;
    color: var(--nex-gray);
    margin-bottom: 0.5rem;
  }
  
  .post-title {
    margin-bottom: 1rem;
    font-size: 1.5rem;
  }
  
  .post-title a {
    color: var(--nex-black);
    text-decoration: none;
  }
  
  .post-title a:hover {
    color: var(--nex-red);
  }
  
  .post-excerpt {
    color: var(--nex-dark);
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }
  
  .read-more {
    font-weight: 600;
    color: var(--nex-red);
  }
  
  .read-more:hover {
    text-decoration: underline;
  }
</style>
